#include "parameter.hpp"

// This file is currently empty as Parameter is a simple struct-like class.
// If you add member functions in the future, implement them here.

// For example, if you add a constructor:
/*
Parameter::Parameter(std::string name, std::string type, std::string description)
    : name(std::move(name)), type(std::move(type)), description(std::move(description)) {}
*/

// Or if you add a method:
/*
bool Parameter::isValid() const {
    return !name.empty() && !type.empty();
}
*/
